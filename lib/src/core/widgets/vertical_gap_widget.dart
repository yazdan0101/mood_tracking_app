import 'package:flutter/material.dart';

class VerticalGapWidget extends StatelessWidget {
  const VerticalGapWidget({super.key});

  @override
  Widget build(final BuildContext context) => const SizedBox(
        height: 20,
      );
}
